apiVersion: syseleven-metakube.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    source: Secret
    secretRef:
      name: metakube-credentials
      namespace: crossplane-system
      key: credentials
